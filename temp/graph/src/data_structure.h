#inndef _DATA_STRUCTURE_H
#define _DATA_STRUCTURE_H

/* The following structures are used to define the data to be contained in each of the node and edge node, eg: node properties, such as weight, etc,and for edgethe direction and weights, and any other information such as costs etc. */

typedef struct vertex_data{
// fill in the data to be stored

} v_data;

typedef struct edge_data{
// fill in the edge parameters to be stored

} e_data;
#endif
